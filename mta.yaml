_schema-version: "3.2"
ID: demoappdemofiori
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: demoappdemofiori-destination-content
  type: com.sap.application.content
  requires:
  - name: demoappdemofiori-destination-service
    parameters:
      content-target: true
  - name: demoappdemofiori-repo-host
    parameters:
      service-key:
        name: demoappdemofiori-repo-host-key
  - name: demoappdemofiori-uaa
    parameters:
      service-key:
        name: demoappdemofiori-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: demoappdemofiori_html_repo_host
          ServiceInstanceName: demoappdemofiori-html5-service
          ServiceKeyName: demoappdemofiori-repo-host-key
          sap.cloud.service: demoappdemofiori
        - Authentication: OAuth2UserTokenExchange
          Name: demoappdemofiori_uaa
          ServiceInstanceName: demoappdemofiori-xsuaa-service
          ServiceKeyName: demoappdemofiori-uaa-key
          sap.cloud.service: demoappdemofiori
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: demoappdemofiori-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: demoappdemofiori-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - demoappdemofiori.zip
      name: demoappdemofiori
      target-path: resources/
- name: demoappdemofiori
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: demoappdemofiori-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: demoappdemofiori-destination-service
    service-plan: lite
- name: demoappdemofiori-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: demoappdemofiori-xsuaa-service
    service-plan: application
- name: demoappdemofiori-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: demoappdemofiori-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
